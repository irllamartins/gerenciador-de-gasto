<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GerenciadorDeGasto" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GerenciadorDeGasto" parent="Base.Theme.GerenciadorDeGasto" />

    <style name="Theme.GerenciadorDeGasto.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.GerenciadorDeGasto.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.GerenciadorDeGasto.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>